{% extends "admin.twig" %}
{% block content %} 
    <br />
    <div id="div-lista-produto">
        <div class="row">
            <div class="col s6">
                <a href="{{BASE_URL}}/bairros/novo"><i class="material-icons">add_box
                    </i> Novo</a>  
            </div>
        </div>     
        <div id="table-datatables">
            <div class="row">
                <div class="col s10">
                    <table id="data-table-simple" class="responsive-table display" cellspacing="0">
                        <thead>
                            <tr>
                                <th>Nome do bairro</th>
                                <th>Taxa</th>       
                                <th>Ação</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>Nome do bairro</th>
                                <th>Taxa</th>       
                                <th>Ação</th>
                            </tr>
                        </tfoot>
                        <tbody>                        
                            {% for bai in bairros %}                    
                                <tr>                                                   
                                    <td>{{ bai.bai_nome }}</td>
                                    <td>{{ bai.bai_taxa }}</td>
                                    <td>
                                        <a href="{{BASE_URL}}/bairros/editar/{{bai.bai_codigo}}">
                                            <i class="material-icons">edit</i>
                                        </a>
                                        <a href="javascript:if(confirm('Deseja excluir este bairro ?')) {location='{{BASE_URL}}/bairros/excluir/{{bai.bai_codigo}}';}">
                                            <i class="material-icons">delete_forever</i>
                                        </a>  
                                    </td>
                                </tr>
                            {% endfor %}                         
                        </tbody>                    
                    </table>
                    <input type="hidden" id="BASE_URL" value="{{BASE_URL}}" />
                </div>
            </div>
        </div>   
    </div>
{% endblock content %}